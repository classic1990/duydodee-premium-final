name: Code Quality & Security

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  # ========================================
  # LINT & CODE QUALITY JOB
  # ========================================
  lint:
    name: Lint & Code Quality
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
      
      - name: Setup Node.js
        uses: actions/setup-node@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          node-version: '18.x'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ESLint
        run: npm run lint
        continue-on-error: true
      
      - name: Check for lint errors
        run: |
          if npm run lint; then
            echo "No lint errors found"
          else
            echo "Lint errors still present after auto-fix"
            exit 1
          fi
      
      - name: Upload lint results
        uses: actions/upload-artifact@f43a0e5ff2bd294095638e18286ca9a3d1956744
        if: always()
        with:
          name: lint-results
          path: eslint-report.html

  # ========================================
  # SECURITY SCAN JOB
  # ========================================
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
      
      - name: Setup Node.js
        uses: actions/setup-node@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          node-version: '18.x'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run npm audit
        run: npm audit --audit-level=high
        continue-on-error: true
      
      - name: Run npm audit fix
        run: npm audit fix
        continue-on-error: true
      
      - name: Check for secrets
        uses: trufflesecurity/trufflehog-action@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
      
      - name: Security scan with Snyk
        uses: snyk/actions/node@b98d498629f1c368650671344b2161ff86cdc6a1
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  # ========================================
  # CODE COVERAGE JOB
  # ========================================
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
      
      - name: Setup Node.js
        uses: actions/setup-node@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          node-version: '18.x'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests with coverage
        run: npm run test:coverage
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@f43a0e5ff2bd294095638e18286ca9a3d1956744
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella